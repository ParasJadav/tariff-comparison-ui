name: Build and Deploy Angular App to Azure Web App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'

      - name: Install Angular CLI
        run: npm install -g @angular/cli

      - name: Install dependencies
        run: npm install

      - name: Build Angular app
        run: npm run build -- --configuration production

      # Optional: Uncomment to run tests
      # - name: Run tests
      #   run: npm run test -- --watch=false --browsers=ChromeHeadless

      - name: Upload artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: tariff-comparison-app
          path: ./dist/tariff-comparison

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: tariff-comparison-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_733EBF5659C84F3F8765A2006564EA21 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_0A4EF044A0614B7D9B819028FBF51F6E }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_68039E7C658546AB87DC43AD57FDF9A6 }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'tariff-comparison-ui'
          slot-name: 'Production'
          package: .
